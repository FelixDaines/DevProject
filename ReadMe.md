## Website to track college bars attended

# Store which bars attended using local storage
Local storage contains keys of each college and value which is number of times that bar attended. Defaults to zero
